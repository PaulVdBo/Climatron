name: Supabase Heartbeat

on:
  schedule:
    - cron: '0 12 */5 * *'
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Test netwerk
        run: |
          echo "=== DNS test ==="
          nslookup supabase.com || true
          nslookup erocbubyvpxnvkpjnlbd.supabase.co || true
          echo "=== Ping supabase.com ==="
          curl -I https://supabase.com || true
          echo "=== Klaar ==="
